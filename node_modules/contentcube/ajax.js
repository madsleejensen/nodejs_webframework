var http = require("http");
var url = require("url");


// todo setup convention for modules.
exports.doGet = function(url, params, callback) {
	
	var partsInUrl = url.parse(url);
	var options = {
		host: ''
	};
	
	
};